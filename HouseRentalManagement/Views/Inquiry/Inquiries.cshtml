@model HouseRentalManagement.Models.InquiryViewModels.ListInquiryViewModel

<div class="container">
	<div class="row">
		<div class="col-md-12">
			<table class="table table-responsive table-hover inquiry-table">
				<thead>
					<tr>
						<th>Date</th>
						<th>Name</th>
						<th>Phone Number</th>
						<th>Email Address</th>
						<th class="text-center">Read?</th>
						<th class="text-center">Message</th>
						<th style="width: 25px;"></th>
					</tr>
				</thead>
				<tbody>
					@foreach (var item in Model.Inquiries)
					{
						<tr id="@item.InquiryId">
							<td>@item.InquiryDate.ToString("MMM dd, yyyy hh:mm tt")</td>
							<td>@item.Name</td>
							<td>@item.PhoneNumber</td>
							<td>@item.Email</td>
							<td class="text-center">
								@if (item.IsRead)
								{<i class="fa fa-check"></i>}
								else
								{<i class="fa fa-close"></i>}
							</td>
							<td class="text-center">
								<a class="btn btn-primary" onclick="Hrm.InquiryIndex.loadMessage('@item.InquiryId');"><i class="fa fa-ellipsis-h"></i></a>
							</td>
							<td class="text-center">
								<a class="btn btn-danger" onclick="Hrm.InquiryIndex.deleteInquiry('@item.InquiryId');" title="Delete"><i class="fa fa-trash-o"></i></a>
							</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>